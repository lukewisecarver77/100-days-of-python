import math
test_h = int(input("height of wall: "))
test_w = int(input("Width of wall: "))
coverage = 5
def paint_calc(height=test_h, width=test_w, cover=coverage):
  answer = math.ceil((test_h * test_w)/coverage)
  print(f"You will need {answer} cans of paint for your project.")

paint_calc()
